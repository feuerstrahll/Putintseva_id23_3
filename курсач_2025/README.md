# 📚 Архивное веб-приложение

Современное full-stack веб-приложение для управления архивными фондами с аутентификацией, разграничением прав доступа и расширенным функционалом поиска.

## ✨ Основные возможности

- 🔐 **Аутентификация и авторизация** - JWT токены, 3 роли (Администратор, Архивариус, Исследователь)
- 📁 **Управление фондами** - CRUD операции для фондов, описей и архивных единиц
- 🔍 **Расширенный поиск** - поиск по названию, датам, ключевым словам с фильтрацией и сортировкой
- 📊 **Статистика и аналитика** - визуализация данных с графиками (Recharts)
- 📝 **Система заявок** - исследователи могут подавать заявки на просмотр и оцифровку
- 👥 **Управление пользователями** - администратор может изменять роли пользователей
- 📋 **Журнал аудита** - отслеживание всех действий в системе
- 🎨 **Современный UI** - адаптивный интерфейс с toast-уведомлениями
- 🛡️ **Обработка ошибок** - страницы 404/403/500, валидация форм

---

## 🛠 Технологический стек

### Frontend
- **React 18** + **TypeScript** - UI библиотека с типизацией
- **Vite** - быстрая сборка и dev-сервер
- **TanStack Query** - управление серверным состоянием
- **React Router** - маршрутизация SPA
- **Recharts** - графики и визуализация данных

### Backend
- **NestJS** + **TypeScript** - прогрессивный Node.js фреймворк
- **TypeORM** - ORM для работы с базой данных
- **PostgreSQL 15** - реляционная база данных
- **JWT** + **Passport** - аутентификация и авторизация
- **class-validator** - валидация входных данных
- **Swagger** - автоматическая документация API

### Инфраструктура
- **Docker** + **Docker Compose** - контейнеризация
- **Monorepo** структура

---

## 🚀 Быстрый старт

### Требования
- **Docker** и **Docker Compose** (рекомендуется)
- Или **Node.js 18+** и **PostgreSQL 15+** (для локальной разработки)

### Запуск за 3 шага

```bash
# 1. Запуск всех сервисов
docker-compose up -d

# 2. Дождитесь запуска (30-60 секунд)
docker-compose logs -f backend
# Ждите: "Application is running on..."

# 3. Откройте в браузере
# Frontend: http://localhost:5173
```

### После запуска доступно:
- 🌐 **Frontend**: http://localhost:5173
- 🔌 **Backend API**: http://localhost:3000
- 📖 **Swagger документация**: http://localhost:3000/api/docs
- 🐘 **PostgreSQL**: localhost:5432

---

## 👤 Первая настройка

1. **Зарегистрируйте первого пользователя** на странице `/register`

2. **Назначьте роль администратора** через БД:
```bash
docker exec -it archive_postgres psql -U archive_user -d archive_db

UPDATE users SET role = 'admin' WHERE email = 'ваш_email@example.com';
\q
```

3. **Перелогиньтесь** для получения прав администратора

---

## 👥 Роли пользователей

| Роль | Описание | Права доступа |
|------|----------|---------------|
| 🔍 **Исследователь** (researcher) | Базовый доступ | • Поиск и просмотр единиц хранения<br>• Создание заявок на просмотр/оцифровку<br>• Просмотр своих заявок<br>• Базовая статистика |
| 📋 **Архивариус** (archivist) | Управление данными | • Все права Исследователя<br>• CRUD фондов, описей, единиц хранения<br>• Управление заявками (изменение статуса)<br>• Полная статистика |
| 👑 **Администратор** (admin) | Полный доступ | • Все права Архивариуса<br>• Управление пользователями (изменение ролей)<br>• Просмотр журнала аудита<br>• Доступ ко всем функциям |

---

## 📂 Структура проекта

```
archive-app/
├── backend/                # NestJS REST API
│   ├── src/
│   │   ├── auth/          # Аутентификация (JWT, RBAC)
│   │   ├── users/         # Управление пользователями
│   │   ├── fonds/         # Фонды
│   │   ├── inventories/   # Описи
│   │   ├── records/       # Единицы хранения
│   │   ├── keywords/      # Ключевые слова
│   │   ├── digital-copies/# Цифровые копии
│   │   ├── requests/      # Заявки исследователей
│   │   ├── stats/         # Статистика
│   │   ├── audit/         # Журнал аудита
│   │   └── common/        # Общие компоненты
│   └── package.json
│
├── frontend/              # React SPA
│   ├── src/
│   │   ├── api/          # API клиенты (axios)
│   │   ├── components/   # Переиспользуемые компоненты
│   │   ├── contexts/     # React контексты (Auth, Toast)
│   │   ├── pages/        # Страницы приложения
│   │   └── types/        # TypeScript типы
│   └── package.json
│
└── docker-compose.yml     # Docker конфигурация
```

---

## 🗄 База данных

TypeORM автоматически создает таблицы при первом запуске.

### Основные таблицы:
- `users` - пользователи (email, passwordHash, role)
- `fonds` - фонды (code, title, description)
- `inventories` - описи (number, title, fondId)
- `records` - единицы хранения (refCode, title, dates, accessLevel)
- `keywords` - ключевые слова для поиска
- `record_keywords` - связь M:N (записи ↔ ключевые слова)
- `digital_copies` - цифровые копии документов
- `requests` - заявки исследователей (type, status)
- `audit_logs` - журнал аудита всех действий

### Связи между таблицами:
```
Fond 1→N Inventory
Inventory 1→N Record
Record N↔M Keyword
Record 1→N DigitalCopy
Record 1→N Request
User 1→N Request
```

---

## 🔌 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход (возвращает JWT токен)

### Управление данными
- `GET /api/fonds` - Список фондов
- `GET /api/inventories` - Список описей
- `GET /api/records` - Список единиц хранения
- `GET /api/records/search` - Поиск с фильтрами
- `GET /api/keywords` - Ключевые слова

### Заявки
- `POST /api/requests` - Создать заявку
- `GET /api/requests` - Список заявок (фильтруется по роли)
- `PATCH /api/requests/:id` - Изменить статус (admin/archivist)

### Администрирование
- `GET /api/users` - Список пользователей (admin)
- `PATCH /api/users/:id` - Изменить роль (admin)
- `GET /api/audit` - Журнал аудита (admin)
- `GET /api/stats` - Статистика

**Полная документация**: http://localhost:3000/api/docs

---

## 🎨 Основные страницы

| Маршрут | Описание | Доступ |
|---------|----------|--------|
| `/login` | Вход в систему | Публичная |
| `/register` | Регистрация | Публичная |
| `/` | Dashboard (статистика) | Все роли |
| `/records` | Единицы хранения с поиском | Все роли |
| `/fonds` | Управление фондами | Archivist, Admin |
| `/inventories` | Управление описями | Archivist, Admin |
| `/requests` | Заявки | Все роли |
| `/stats` | Статистика с графиками | Все роли |
| `/users` | Управление пользователями | Admin |
| `/audit` | Журнал аудита | Admin |
| `/about` | Об авторе | Все роли |

---

## 🔍 Функциональность поиска

### Параметры поиска:
- **Текстовый поиск** - по названию, аннотации, шифру
- **Фильтр по фонду** - выбор конкретного фонда
- **Фильтр по описи** - выбор описи
- **Фильтр по датам** - диапазон дат (от/до)
- **Фильтр по ключевым словам** - множественный выбор
- **Фильтр по уровню доступа** - публичный/ограниченный
- **Сортировка** - по любому полю (ASC/DESC)
- **Пагинация** - постраничный вывод результатов

Пример запроса:
```
GET /api/records/search?search=документ&fondId=1&dateFrom=1920&sortBy=title&page=1&limit=20
```

---

## 🔐 Безопасность

1. **JWT Authentication** - Bearer токены с истечением срока
2. **RBAC** - Role-Based Access Control на уровне роутов
3. **Bcrypt** - хеширование паролей (невозможно расшифровать)
4. **Валидация** - class-validator на всех входящих данных
5. **CORS** - настроен для frontend домена
6. **SQL Injection защита** - параметризованные запросы TypeORM
7. **HTTP Exception Filter** - глобальная обработка ошибок
8. **ErrorBoundary** - перехват React ошибок на frontend

---

## 🧪 Разработка

### Локальный запуск (без Docker)

**Backend:**
```bash
cd backend
npm install
npm run start:dev
```

**Frontend:**
```bash
cd frontend
npm install
npm run dev
```

### Linting
```bash
# Backend
cd backend && npm run lint

# Frontend
cd frontend && npm run lint
```

### Полезные Docker команды
```bash
# Статус контейнеров
docker-compose ps

# Логи в реальном времени
docker-compose logs -f

# Перезапуск
docker-compose restart backend
docker-compose restart frontend

# Остановка с сохранением данных
docker-compose down

# Остановка с удалением БД
docker-compose down -v

# Пересборка контейнеров
docker-compose up --build
```

### Подключение к БД
```bash
# Через psql
docker exec -it archive_postgres psql -U archive_user -d archive_db

# Просмотр таблиц
\dt

# Просмотр пользователей
SELECT id, email, "fullName", role FROM users;

# Выход
\q
```

---

## 📊 Статистика и графики

Приложение включает визуализацию данных:
- **Общие показатели** - количество фондов, описей, единиц, пользователей
- **Статистика заявок** - по статусам (новые, в работе, выполнено)
- **Распределение по фондам** - круговая диаграмма и гистограмма
- **Распределение по годам** - линейный график и столбчатая диаграмма

Библиотека: **Recharts**

---

## 🛡️ Обработка ошибок

### Backend
- `HttpExceptionFilter` - глобальный перехват всех исключений
- `ValidationPipe` - автоматическая валидация DTO
- Логирование ошибок с stack trace
- Структурированные ответы с кодами ошибок

### Frontend
- **ErrorBoundary** - перехват React ошибок
- **Toast уведомления** - success/error/info/warning
- **Страницы ошибок**:
  - 404 - Страница не найдена
  - 403 - Доступ запрещён
  - 500 - Ошибка сервера
- **Axios interceptor** - автоматический logout при 401
- **Валидация форм** - HTML5 + кастомная валидация

---

## 🎯 Функциональные требования методички

Проект полностью соответствует требованиям курсовой работы:

- ✅ **1.1** - Стек: Node.js, React, PostgreSQL, REST API, веб-приложение
- ✅ **1.2** - Регистрация/авторизация, JWT, без гостевого доступа
- ✅ **1.3** - Табличное представление с пагинацией
- ✅ **1.4** - CRUD операции (Create, Read, Update, Delete)
- ✅ **1.5** - Поиск и вывод записей с фильтрами
- ✅ **1.6** - Сортировка по различным критериям
- ✅ **1.7** - Разделение ролей с разными интерфейсами и правами
- ✅ **1.10** - Обработка ошибок, информативные сообщения, устойчивость

---

## 📝 Troubleshooting

### Порт занят
```bash
# Windows
netstat -an | findstr "3000 5173 5432"

# Изменить порты в docker-compose.yml
```

### База данных не создается
```bash
docker-compose down -v
docker-compose up -d
```

### Контейнер не запускается
```bash
docker-compose logs backend
docker-compose logs frontend
docker-compose logs postgres
```

### Frontend не загружается
1. Проверьте, что backend запущен и доступен
2. Откройте DevTools → Network → проверьте запросы к API
3. Убедитесь, что порт 5173 свободен

---

## 📄 Лицензия

Учебный проект - Курсовая работа

---

## 👨‍💻 Автор

Информация об авторе доступна в приложении: `/about`

---

## 🌟 Особенности реализации

- **TypeScript everywhere** - полная типизация на frontend и backend
- **Модульная архитектура** - каждый модуль независим и переиспользуем
- **REST best practices** - правильные HTTP методы и коды ответов
- **React Query** - эффективное кэширование и управление серверным состоянием
- **Responsive design** - адаптивный интерфейс для разных устройств
- **Modern UI/UX** - интуитивно понятный интерфейс с toast-уведомлениями
- **Production-ready** - готово к развертыванию с Docker

---

**Версия**: 1.0.0  
**Дата**: Ноябрь-Декабрь 2025
